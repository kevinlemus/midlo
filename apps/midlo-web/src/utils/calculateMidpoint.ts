import type { Midpoint } from "../types";

/**
 * Placeholder midpoint calculation.
 * Your MVP uses mock midpoint values, but this keeps the structure clean.
 */
export function calculateMidpoint(): Midpoint {
  return { lat: 0, lng: 0 };
}
