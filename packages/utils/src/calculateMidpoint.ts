import type { Coordinates, MidpointResult } from "@midlo/shared-types";

export function calculateMidpoint(
  a: Coordinates,
  b: Coordinates,
): MidpointResult {
  // TODO: implement midpoint calculation later (no business logic in scaffold)
  return { coordinates: { lat: 0, lng: 0 } };
}
